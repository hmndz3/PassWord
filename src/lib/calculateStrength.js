// STUB — fase RED del TDD.
// La implementación real se agrega en el siguiente commit (fase GREEN).
// Por ahora lanza para que todos los tests fallen explícitamente.
export function calculateStrength(_password) {
  throw new Error('calculateStrength: aún no implementado (fase RED)');
}
